package cn.t.ytten.core.channel.initializer;

import cn.t.ytten.core.channel.ChannelContext;
import cn.t.ytten.core.channel.Channelnitializer;

import java.nio.channels.ServerSocketChannel;

public class ServerChannelInitializer implements Channelnitializer<ServerSocketChannel> {
    @Override
    public void initChannel(ChannelContext ctx, ServerSocketChannel serverSocketChannel) throws Exception {

    }
}
